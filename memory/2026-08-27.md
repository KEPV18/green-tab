<!-- notes:auto -->

- [[memory/2026-08-27/sheets-automation]] — Google Sheets automation pipeline fully implemented: sheet_extractor.py (Playwright browser), sheet_parser.py (TSV/CSV parser), fetch_team_data.py (orchestrator). CSV download works via expect_download. Team Scores tab is gid=87009911. Name mapping from ID_Name column. Month from sheet title. AHT values empty in sheet. Cron job set up (8 AM daily). Commit b96b831 pushed to main.

<!-- /notes:auto -->
## Supabase Migration — COMPLETE

### What was done:
1. **Migration applied**: `team_metrics` table created in Supabase with:
   - Columns: id, month, report_date, agent_email, agent_name, bamboo_id, team_lead, csat, productivity, fcr, chat_aht, genesys_aht, chat_handling_time, avg_group_basket_time, escalation_rate, adherence, closed_after_resolution, closed_tickets_pct, deescalation_rate, occupancy, concurrency, irt_replier, shrinkage, utilization, source, fetched_at, created_at, updated_at
   - UNIQUE constraint on (month, agent_email)
   - RLS enabled: anon SELECT only, service_role full access
   - `updated_at` auto-trigger

2. **13 August 2026 team members upserted**:
   - All CSAT values from Tab 0 percentage column (not raw scores)
   - Chat AHT = "Average basket time" (NOT Genesys AHT)
   - Missing values stored as NULL (never 0)
   - Merged data from Sheet19 + Tab 0 for maximum coverage

3. **Frontend migrated to Supabase primary**:
   - `googleSheets.ts` rewritten: Supabase first → localStorage cache → team-data.json fallback
   - `@supabase/supabase-js` installed as dependency
   - Floor averages computed client-side from Supabase data

4. **Security verified**:
   - service_role key: NEVER in frontend, NEVER in git, NEVER in build
   - Only VITE_SUPABASE_ANON_KEY (public) in frontend bundle
   - RLS: anon SELECT only, INSERT/UPDATE/DELETE blocked (verified)
   - scripts/.env.local stores service_role key (gitignored)

5. **Daily fetch pipeline updated**:
   - `fetch_team_data.py` now calls `supabase_upsert.py` after local save
   - Pipeline: Google Sheet → Playwright → Parse → Validate → Save local JSON → Upsert Supabase

6. **Production deployed**:
   - https://karma-tracker-buddy-dun.vercel.app
   - Vercel env vars: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_SUPABASE_PROJECT_ID
   - Vercel secret: SUPABASE_SERVICE_ROLE_KEY (production only)

7. **Cleanup**:
   - Removed SUPABASE_AUDIT_REPORT*.md from git
   - Removed dist/ from git tracking
   - Removed .env from git (was committed, now deleted)
   - Added dist/, supabase/.temp/, scripts/.env.local to .gitignore

## Dashboard Logic Overhaul — COMPLETE

### Changes Made:
1. **Chat Team Filtering**: 4 agents excluded from ranking (display-only, data preserved in Supabase):
   - Abdallah Abdallah → PHONE team
   - Mohamed Mohamed 27 → CONSULTATION team  
   - Ahmed Elkhodary → TERMINATED
   - Abdullah Riad → TERMINATED

2. **Close Rate Mapping CONFIRMED**: 
   - `closed_tickets_pct` = Sheet "Closed tickets, %" = **Close Rate** ✅
   - `closed_after_resolution` = Sheet "Closed after resolution, %" = separate metric ✅
   - These are DIFFERENT values (e.g. Ahmed Ahmed: 39.1% vs 58.0%)

3. **Ranking Formula** (weighted composite):
   - Productivity: 35% (higher = better)
   - CSAT: 30% (higher = better)
   - Close Rate: 20% (higher = better)
   - ABT: 15% (LOWER = better, direction normalized)

4. **Full Metric Display**: Every agent now shows all available metrics in expandable detail view.

5. **Floor Averages**: Computed over all 13 members for context; ranking shows 9 active Chat agents only.

6. **No Schema Changes Required**: All fields already existed in Supabase.

### Verification:
- Active Chat team: 9 agents ✅
- Close Rate = closed_tickets_pct (NOT closed_after_resolution) ✅
- CSAT = percentage (NOT raw score count) ✅
- ABT = Average Basket Time (NOT Genesys AHT) ✅
- Production deployed: https://karma-tracker-buddy-dun.vercel.app ✅
- Build: tsc clean, vite build success ✅
- Commit: 7bafb90
